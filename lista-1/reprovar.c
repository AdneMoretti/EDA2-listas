#include <stdio.h>
#include <string.h> 

int main(){
    char nome[21];
    int acertos;
    scanf(" %s %d", acertos)
    while(scanf(" %s %d", acertos)!= EOF){
        if(c[0] == 's') aux+=1;
        count++;
        if (count == 10){
            count=0;
            if (aux>=2) soma+=1;
            aux=0;
        }
    }
    printf("%d\n", soma);
    return 0;
}
